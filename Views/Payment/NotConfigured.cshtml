@{
    ViewData["Title"] = "Payment";
}
<div class="container my-4">
    <h3>Payment not configured</h3>
    <p>This demo build does not have card payments configured. Use Cash on Delivery or configure a payment provider in <code>appsettings.json</code> and implement the connector in <code>PaymentService</code>.</p>
    <a asp-controller="Home" asp-action="Menu" class="btn btn-primary">Back to Menu</a>
</div>
